{
  "name": "brooklyn-store",
  "version": "1.0.0",
  "description": "## Camera-store",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\"  \"npm run dev --prefix frontend\" ",
    "build": "cd backend && npm install && npm run build  && cd ../frontend && npm install && npm run build",
    "start": "TG_ALLOW_MIXED=ALLOW  node backend/build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/basir/Michaecyber"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/basir/Michaecyber"
  },
  "homepage": "https://github.com/Michaecybere",
  "devDependencies": {
    "concurrently": "^8.0.1"
  },
  "dependencies": {
    "@typegoose/typegoose": "^12.6.0",
    "mongoose": "^8.5.1"
  }
}
